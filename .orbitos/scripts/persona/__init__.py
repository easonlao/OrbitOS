# OrbitOS Dynamic Persona Layer — package marker.
#
# Implementation modules live alongside this file:
#   persona_source.py  shared source-of-truth model (load/save, zones)
#   mbti.py            first-run questionnaire + seed scoring
#   baseline.py        first-run baseline flow (#7)
#   calibrate.py       behavior-evidence calibration loop (#8)
#   project.py         projections into collab/state/direction views (#9, #10)
