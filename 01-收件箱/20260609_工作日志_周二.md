---
title: "2026-06-09 周二 工作日志"
type: "worklog"
topic: "work"
workspace: "02-日记"
created: "2026-06-09 00:00:37"
modified: "2026-06-09 17:49:00"
tags: ["worklog", "work", "event-capture"]
source: "agent"
status: "active"
---

# 2026-06-09 周二 工作日志

## 今日重点

- 部署 CookieCloud 定时同步 FNOS Docker 容器中的 Google cookies。
- 重构工作日志，废弃 `todos/` 并完成双轨制 Todo 归一化。

## 今日Todo

- [x] [高优先] 部署 CookieCloud（FNOS Docker + Chrome 插件 + auth.json 转换脚本）——解决 NotebookLM cookies 24h 过期问题 ✅
- [x] [高优先] 验证 Deriver patch 在新消息上的效果（custom_instructions 方案） ✅
- [x] [中优先] 搜索引擎路由 skill 的端到端测试 ✅
- [x] [中优先] NotebookLM 深度调研工作流 skill 实现 ✅
- [x] [中优先] 中转站/机场进一步调研或自建 VPS 方案 —— 已评估：费用过高，暂缓 ✅
- [x] [低优先] Firecrawl Extract 功能重新验证 —— 已更换新 key，并通过 Firecrawl API + Hermes `web_extract` 双重测试 ✅

## 重点记录

> 时间基准：23:00 为日切分点。时间段：白天(08-18) / 晚上(18-00) / 凌晨(00-08)

### 晚上（18:00-00:00，跨 23:00 属本日）

### 23:23 — NotebookLM Cookie 过期问题发现与方案评估

**为什么做**：NotebookLM cookies ~24h 过期，导致 MCP 认证失败
**怎么做的**：评估 CookieCloud、sync-your-cookie、cookie-share 三个方案，推荐 CookieCloud（已有 FNOS Docker 环境、支持定时同步、中文社区、按域名过滤）
**改了什么**：无文件变更，决策记录

### 23:47 — 搜索引擎工具链配置

**为什么做**：需要多搜索引擎支持以覆盖不同查询场景
**怎么做的**：配置 Tavily（主搜索）、SearXNG（MCP 备用）、AnySearch（MCP 垂直）
**改了什么**：MCP 配置文件

### 23:51 — 网络工具调研

**为什么做**：评估中转站/机场方案
**怎么做的**：调研 ai.superdoor.top（WARP 隧道，不推荐）、ai.opendoor.cn（无口碑）、angeworld.cc（Forest 相对最好）
**改了什么**：无文件变更，决策记录

### 23:56 — Docker 容器时区统一

**为什么做**：Honcho 时间戳显示 UTC，需要统一为上海时间
**怎么做的**：为 9 个 Docker 容器添加 `TZ=Asia/Shanghai`
**改了什么**：Docker compose 配置文件

### 凌晨（00:00-08:00）

### 00:00 — 工作日志时间戳修正与规范化

**为什么做**：发现多个时间戳错误（proofrail、Deriver、CookieCloud 归属日期），根因是依赖记忆猜时间
**怎么做的**：创建 `worklog-timeline.py` 脚本从 state.db 提取事件时间线，修正 6月8日和6月9日工作日志
**改了什么**：`scripts/worklog-timeline.py`、`02-日记/工作日志/20260608_*.md`、`02-日记/工作日志/20260609_*.md`

### 00:09 — proofrail 深度分析

**为什么做**：用户质疑三层约束为何都没拦住违规行为
**怎么做的**：分析 proofrail 只检查 tool-call 级证据，不检查工作流合规性；根因：三层约束都是"软约束"
**改了什么**：无文件变更，状态：待用户决定方案方向

### 04:12 — NotebookLM auth.json 生成

**为什么做**：Cookie 过期导致 NotebookLM MCP 认证失败
**怎么做的**：从 CookieCloud 解密 .google.com 等域名的 23 个认证 cookie，导出为 auth.json
**改了什么**：`~/.notebooklm-mcp/auth.json`

### 04:50 — 通用 Cookie 导出工具开发

**为什么做**：需要一个通用工具从 CookieCloud 按域名导出 cookies，支持多种格式
**怎么做的**：开发 `cookie_export.py`，支持 header/dict/notebooklm/playwright/curl/raw 6 种格式
**改了什么**：`~/.hermes/skills/devops/cookie-cloud/scripts/cookie_export.py`

### 04:55 — cookie-cloud skill 创建

**为什么做**：将通用 cookie 导出工具封装为 skill，供多设备 agent 复用
**怎么做的**：创建 SKILL.md + scripts/，含 cookie_export.py 和 refresh_notebooklm_auth.py
**改了什么**：`~/.hermes/skills/devops/cookie-cloud/SKILL.md`、`scripts/cookie_export.py`、`scripts/refresh_notebooklm_auth.py`

### 05:08 — worklog-writing skill 对齐 OrbitOS

**为什么做**：Hermes worklog-writing skill 与 OrbitOS 框架（event-capture.yaml）不对齐
**怎么做的**：更新 Agent 追责格式（summary/why/how/changed_files）、section 顺序（Git 提交在最末）、删除独立 Agent 产出 section
**改了什么**：`~/.hermes/skills/productivity/worklog-writing/SKILL.md`、`references/worklog-writing.md`

### 白天（08:00-18:00）

### 17:04 — NotebookLM × OrbitOS 双向工作流 skill 升级

**为什么做**：用户指出 NotebookLM 不是单向调研工具，而是数据调研、杂乱信息整理和内容生成工作台；OrbitOS 是提炼内容和输出方向正本库，两者应形成双向循环
**怎么做的**：读取并升级已有 `notebooklm-research-workflow` skill，加入双向循环定位、OrbitOS 框架边界、Studio 产物生成、回流资产类型、语言/locale 显式确认规则，并修复编号与重复章节
**改了什么**：`~/.hermes/skills/research/notebooklm-research-workflow/SKILL.md`

### 17:20 — 住宅/ISP IP 出口方案费用评估

**为什么做**：用户询问自建稳定使用 AI 的家用 IP 成本，需要判断是否值得继续投入
**怎么做的**：区分中国家宽、普通 VPS、Static Residential/ISP Proxy、住宅 VPS、海外真实家宽几类方案，估算月成本与适用场景
**改了什么**：无系统配置变更；决策为费用过高，当前暂缓，不继续推进

### 17:35 — Firecrawl API key 主备切换配置

**为什么做**：用户提供新的 Firecrawl API key，希望设为主 key，并保留原 key 作为额度耗尽后的 fallback，方便后续备查
**怎么做的**：检查 Hermes Firecrawl provider 源码，确认当前只原生读取单个 `FIRECRAWL_API_KEY`；更新 `.env`，将新 key 设为主 key、原 key 设为 `FIRECRAWL_API_KEY_FALLBACK`；创建安全切换脚本并写入 Honcho masked 记录
**改了什么**：`~/.hermes/.env`、`~/.hermes/backups/.env.firecrawl-20260609-173457.bak`、`~/.hermes/scripts/switch-firecrawl-key.py`；Honcho 记录主备 key 的 masked 标识与切换规则

### 17:49 — Firecrawl 新 key 与 Hermes Extract 链路验证

**为什么做**：用户追问新 Firecrawl key 是否实际可用，需要从“配置已替换”升级为“真实 API 和 Hermes 工具链均验证通过”
**怎么做的**：先用 `.env` 当前 `FIRECRAWL_API_KEY` 直接请求 Firecrawl `/v1/scrape` 抓取 `https://example.com`，返回 `http_status=200`、`success=True`、`has_markdown=True`；随后调用 Hermes `web_extract` 抓取同一 URL，返回标题 `Example Domain` 和 markdown 内容
**改了什么**：更新 `今日Todo` 中 Firecrawl Extract 验证项为完成；更新 `问题与风险`，移除“等下月重置”的过期表述

## 关键决策

| 时间 | 决策 | 选择 | 理由 |
|------|------|------|------|
| 23:20 | AnySearch 注册 | 使用免费层 | 1000次/天足够 |
| 23:56 | Docker 时区 | TZ=Asia/Shanghai | Honcho 时间戳显示上海时间 |
| 00:00+ | 工作日志规范 | 已完成→工作日志，未完成→今日Todo | 单一事实源，统一在主工作日志内聚合 |

## 问题与风险

- ~~NotebookLM cookies 24h 过期~~ ✅ 已解决：CookieCloud 自动同步 + auth.json 刷新
- 中转站/机场未找到可靠方案
- Firecrawl 当前使用新主 key 可正常 Extract；原 key 保留为 fallback，但 Hermes provider 原生不支持自动 fallback，需要脚本手动切换或后续改源码

## 明日计划

## Git 提交

- 2026-06-09 10:25:10 [git_commit:0e1b7d6456d25d653c305a012ee2b50c04f5d197] `OrbitOS` main 0e1b7d6：docs: define journal boundaries and agent log attribution；文件：.orbit/schema/event-capture.yaml, .orbit/schema/event-log.yaml, 00-系统/Skills/capture/SKILL.md, 00-系统/Skills/orbit-vault/scripts/orbit-vault.mjs, 00-系统/Skills/review/scripts/review_engine.py, 00-系统/Skills/worklog/SKILL.md, 00-系统/Skills/worklog/scripts/worklog_manager.py, 00-系统/Skills/workspace-journal/SKILL.md, 00-系统/规范/13_人机协作与日志追责规范.md, 00-系统/运行时/hooks/claude-stop-hook.sh, 02-日记/WORKSPACE.md, MANUAL.md, README.md；记录方：`git-hook`；写入方式：`git-hook`

- 2026-06-09 10:44:41 [git_commit:b682c5174a782ee001d02f09670abfccca8b01cf] `OrbitOS` main b682c51：docs: deprecate todos folder and unify to worklog daily todo；文件：.gitignore, 00-系统/Skills/reflect/manifest.yaml, 00-系统/规范/13_人机协作与日志追责规范.md, 02-日记/WORKSPACE.md, 02-日记/todos/.gitkeep, MANUAL.md；记录方：`git-hook`；写入方式：`git-hook`
