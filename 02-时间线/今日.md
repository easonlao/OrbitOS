---
title: 今日
area: timeline
purpose: status
lifecycle: active
created: 2026-06-11
updated: 2026-06-14
tags:
  - orbitos
  - timeline
---

# 今日

## 1. 总览

### 当前判断

- OrbitOS 已有可运行系统基线:入口、时间线、Agent Profile、schema、workflow、eval 都已建立。
- 今天的主线是审查 workflow 关联是否过重,先把 Startup Sync、Progress Sync、Experience Capture、Rule Evolution 的触发边界拆清楚。
- `今日.md` 是用户主入口;待确认事项应收拢在这里展示。
- 根 `AGENTS.md` 已重排为:启动顺序、工作流入口、规则入口、执行边界、同步与输出、开发边界。
- 新增 Agent Onboarding:真实 agent 第一次接入时,先只读 Startup Sync;未注册则停下收集字段,用户确认后再注册。
- Workflow checklist 已落地:workflow 定义核对清单,event 记录执行结果,今日只投影异常和关键摘要。
- 当前同步已按 checklist 真实执行,结果写入 event;今日只保留状态摘要。

### 当前阶段

- 单 agent 接入闭环继续打磨中。
- 新 agent 接入语义已收紧:不再叫"初始化数据库",改为"Agent Onboarding / 接入注册"。
- Startup Sync 已补充 Agent Profile 回读要求：已注册 agent 行动前必须读取自己的经验、踩坑、待确认来源和 Learned Rule 使用记录。
- Experience Capture 已作为 Rule Evolution 的前置输入层:agent 在出现错误、返工、用户纠正、validation 失败或明显有效做法时,必须先记录到自身 profile。
- Rule Evolution Workflow 已补齐闭环:agent profile 积累、learned index 总表、今日待确认投影、core 提升确认、失败回退。
- Workflow checklist 已形成最小闭环:规则、schema、eval、核心 workflow、真实 event 全部落地。
- 今日收口判断:阶段 5 的 Role / Thinking Mode 尚未开始;阶段 5 前置的 Rule Evolution Workflow 已完成骨架,但还未完成 learned rule 输出链路。
- 2026-06-14 新增判断: workflow 不宜继续强绑定为一条重流程;Startup Sync / Progress Sync 保持自动,Experience Capture / Rule Evolution 应按条件触发。
- 2026-06-14 追加判断: OrbitOS 最小内核应先收敛为“收件箱入库 + 自检审核”,用来解决系统崩溃的根因:文件放置混乱。
- 2026-06-14 实现进展:最小内核已进入系统层首版实现,下一步需要用真实收件箱小批次试跑。
- 2026-06-14 试跑进展:已用 2 个真实技术类收件箱文件完成首个 inbox-ingest 小批次,并通过 vault-audit/validation 检查。
- 2026-06-14 脚本层调整:validation 主实现已切换为 Python,PowerShell 降级为 wrapper,Node 保留 fallback。
- 2026-06-14 运行环境约定:已新增 Runtime Contract 和 `env-check.py`;当前 Codex runtime 检查状态为 `ok`。
- 2026-06-14 Hermes 接入:已切换到 `/home/lyx/orbitos` 完整工作副本,完成 Agent Onboarding；runtime status 为 `degraded`（PowerShell 不可用）,Python validation 通过。
- 2026-06-14 Hermes 小任务:Hermes 已完成 `03-项目/MAP.md` 低风险写入任务;已补默认任务边界规则和 Progress Sync 自检。
- 2026-06-14 Hermes 定时任务判断:可先从 validation watchdog 等只读/no-agent 任务开始测试稳定性;已补无人值守定时任务边界规则。

### 系统健康

- `AGENTS.md` 当前 140 行，仍低于 200 行约束。
- validation eval 已通过：Python、PowerShell wrapper、Node fallback 均为 `Validation eval passed: 20 case(s).`
- runtime check 已通过：Codex 为 `ok`；Hermes 为 `degraded`（Python 3.11.15、Node v24.16.0、Git 2.43.0 可用，PowerShell 不可用）。
- 可见层已扫描，未发现指向 `.orbitos/` 的 Obsidian 双链残留。
- Nova 暴露的 HanaAgent sandbox 限制已处理：`pwsh` 不可用时优先运行 `python .orbitos/scripts/run-validation.py`，Python 不可用时再用 Node fallback。

## 2. 待确认

### 当前需要你确认

- **systemd 服务管理方案与操作边界**:Hermes 管理 user systemd 服务的操作边界协定,建议后续归入 OrbitOS 运维子项目。
- **收件箱迁移方向**:Nova dry run 已完成 6 个主题簇、57 个文件盘点,需要你确认后才移动或沉淀内容。
- **技术类入库后续方向**:首个 inbox-ingest 小批次中 2 个技术文件已入库并标记 `needs_review`,需要确认后续归入资源、知识或系统运维材料。
- **Workflow 解耦方向**:是否接受经验自检结果枚举 `not_applicable / captured / candidate_only / learned_updated`,并据此更新系统 workflow。

### 规则判断

- `今日.md` 应直接展示当前待确认事项。
- `02-时间线/待确认.md` 不再作为日常入口;后续可降级为归档/backlog,或迁移到各 agent profile。
- 每个 agent 的待确认来源应记录在自己的 `00-系统/agents/{agent_id}.md` 中,再由 Progress Sync 投影到今日。

## 3. 今日进展

### 收件箱盘点

- Nova 执行收件箱 dry run：scan → classify → triage queue → validation → Progress Sync，全链路跑通。
- 前次报告数量从 48 修正为 57，已同步 queue/event/Dashboard。
- 发现关键特征：大部分文件已有 frontmatter workspace 标注，预计可直接按标注迁移。
- 提出 5 个待确认事项（归属结构、目录关系、工作量较大的去向建议）。

### Agent 接入

- Hermes 完成 Agent Onboarding：通过 Ubuntu mapped filesystem 接入，路径 `/home/lyx/orbitos`，IP 10.10.10.33。
- Nova 完成 Agent Onboarding：通过 HanaAgent sandbox 接入，IP 10.10.10.70。
- validation eval 已新增 Node fallback；两套脚本都覆盖 eval、可见 Markdown 双链边界和真实 registry。
- Agent Onboarding 闭环已验证：未注册→停止→确认字段→注册→Progress Sync，全链路跑通。

### Agent 入口

- 重排根 `AGENTS.md`：将工作流入口和规则入口分开，避免 rules/workflows 混插。
- 保留 Startup Sync、Experience Capture、Progress Sync 和开发边界要求。
- 新增 `.orbitos/workflows/agent-onboarding.md`：用于注册已确认的新 agent。
- 在根 `AGENTS.md` 补充新 agent 第一次接入提示词，要求未注册时停止写入并先询问 `agent_id`、部署位置、局域网 IP、接入方式和 OrbitOS 路径。
- 在 `README.zh-CN.md` 和 `README.md` 补充用户可复制的新 agent 首次接入提示词。
- 更新 `.orbitos/workflows/startup-sync.md`：补充"不清楚下一步时"的默认输出。

### 规则分层

- 将执行规则从 `.orbitos/docs/` 迁移到 `.orbitos/rules/core/`:
  - `markdown-writing.md`
  - `readme-writing.md`
  - `git-management.md`
  - `versioning.md`
  - `doc-promotion.md`
- 新增 `.orbitos/docs/README.md`,明确 docs 只放需求、架构、设计理由、未决权衡和实现解释。

### 可见文档

- 修正 `00-系统/MAP.md` 中指向 `.orbitos/docs/` 的 Obsidian 双链,改为普通路径。
- 重写 `今日.md` 为层级式 Dashboard,降低用户阅读混乱。
- 新增可见 Markdown 链接边界 eval:fixture 覆盖 invalid/valid,并扫描真实可见层,禁止指向 `.orbitos/` 的 Obsidian 双链。
- 新增 workflow checklist 标准和 event `checklist` 字段;本次 Progress Sync 已用 checklist event 真实跑通。
- Hermes 已为 `03-项目/MAP.md` 补充首版项目导航草案；内容仅做导航，不记录项目状态。

### Workflow checklist

- 新增 `.orbitos/rules/core/workflow-writing.md`,规定 workflow 必须有执行清单。
- `event.schema.yaml` 新增可选 `checklist` 字段,状态限定为 `done/skipped/failed/blocked`。
- 新增 `event.invalid-checklist-status.yaml`,非法 checklist 状态会被 eval 拒绝。
- `startup-sync`、`progress-sync`、`experience-capture`、`rule-evolution` 已加入执行清单。
- 本次同步生成了带 checklist 的真实 event:`.orbitos/logs/events/20260613_214025_workflow-checklist-baseline.yaml`。

### 路线图收口

- `03-项目/OrbitOS/main/ROADMAP.md` 已更新为当前真实进度。
- 阶段 1 基本完成,继续用真实 agent 验证。
- 阶段 2 dry run 已完成,等待用户确认后才迁移。
- 阶段 3 正在进行,今日已明确项目 STATUS 是状态源。
- 阶段 5 未开始;阶段 5 前置 Rule Evolution Workflow 骨架已完成,输出链路待验证。

### Workflow 解耦审查

- 已新增 OrbitOS 项目审查记录：[[../03-项目/OrbitOS/main/WORKFLOW-DECOUPLING-REVIEW|Workflow 解耦审查]]。
- 当前建议：Startup Sync 和 Progress Sync 保持自动；Experience Capture 只在命中触发条件时写 agent profile；Rule Evolution 只在候选足够通用、原子化、可执行、可验证时进入 learned index。
- 这一步暂未修改 `.orbitos/workflows/`，需要用户确认后再提升为系统层规则。

### 最小内核设计

- 已新增 OrbitOS 项目设计记录：[[../03-项目/OrbitOS/main/MINIMAL-KERNEL-INGEST-AUDIT|最小内核：收件箱入库与自检审核]]。
- 当前判断：`01-收件箱/` 是未处理输入；`01-收件箱/已入库/` 是已处理原始输入；入库索引只做去重和定位，不承担价值判断。
- 系统层首版已实现：`ingest-batch.schema.yaml`、`inbox-ingest.md`、`vault-audit.md`、`ingest/batches/.gitkeep`。
- ingest batch 只保留 `id/date/file/status`，状态为 `ingested / needs_review / discarded`。
- validation 已覆盖 ingest batch 结构和 `01-收件箱/已入库/` 文件记录一致性。

### 最小内核试跑

- 已执行首个真实 inbox-ingest 小批次：`INB-20260614-001`。
- 已移动到 `01-收件箱/已入库/`：
  - `WSL2_Installation_Guide.md`
  - `codex-browser-computer-use-plugin-fix.md`
- 两个文件均标记为 `needs_review`，后续方向已投影到待确认。
- 本次没有创建知识卡片、项目材料或资源条目，只验证“已处理不重复扫描”的最小闭环。

### Runtime 约定

- 已新增 `.orbitos/docs/RUNTIME.md`，定义 OrbitOS agent 最小运行环境。
- 已新增 `env-check.py`，会生成 `.orbitos/state/env/{agent_id}.json` runtime 报告。
- Startup Sync 已接入 runtime check：写入型 workflow 前应确认 runtime 不是 `blocked`。
- Hermes 测试暴露的 blocked case 已补充：如果有 `AGENTS.md` 和 `00-系统/`，但缺少 `.orbitos/`，必须报告工作副本不完整，不得退回旧 `.orbit/`。

### 任务边界

- 已新增 `.orbitos/rules/core/task-boundary.md`。
- 已新增 `.orbitos/rules/core/scheduled-task-boundary.md`，规定定时任务默认只读、no-agent 优先、写入型任务必须声明允许路径。
- 根 `AGENTS.md` 已补充：短请求默认执行最小可逆动作，不把短确认扩展成批量整理。
- Progress Sync 已补充边界自检：scope、user_content、formal_artifact、source_of_truth、validation 必须进入 event checklist。
- 这解决的是“用户无法每次都给 agent 写详细约束”的问题；系统通过默认边界和自检纠偏。

## 4. 可继续

### 推荐下一步

- 确认首个入库小批次中 2 个技术文件的后续归属。
- 确认 workflow 解耦方向：是否采用经验自检结果枚举。
- 若确认，更新系统 workflow，让 Progress Sync 强制自检但不强制 Experience Capture / Rule Evolution 写入。
- 用一次真实踩坑触发 Experience Capture,验证 profile -> 今日 -> Rule Evolution 的输入链路。
- 用下一次 Nova/Hermes 真实小任务继续验证 task-boundary checklist 是否足够。

### 可并行推进

- 复核 Nova 的 inbox triage queue,确认是否按 6 个主题簇迁移。
- 用 Codex/Hermes/HanaAgent 的真实经验试填 `.orbitos/rules/learned/INDEX.md`。
- 用下一个真实 workflow 继续验证 event checklist 到 `今日.md` 的投影规则。

### 暂不推进

- Role card / thinking mode / Hindsight Bridge 暂不推进,等核心 workflow 稳定后再做。

## 5. 各区状态

### 收件箱

- `01-收件箱/` 已完成首轮全量盘点（Nova, 2026-06-13）。
- `01-收件箱/已入库/` 已建立，并完成首个小批次入库试跑（2 个文件，状态均为 `needs_review`）。
- 识别 6 个主题簇共 57 个文件：
  - 交易与做市商方法论（29 件）→ 知识候选
  - Obsidian/AI/Agent 集成（6 件）→ 知识候选
  - 工作日志（5 件）→ 时间线候选
  - 人选天选论（10 件，含 书/目录 8 文件）→ 资源候选
  - 系统与技术笔记（6 件）→ 分散归属
  - 民爆行业分析报告（1 件）→ 资源候选
- 完整文件级建议保存在 `.orbitos/queues/inbox-triage/20260613_215600_inbox-triage-dry-run.yaml`。
- 待用户确认去向方向后执行迁移。

### 项目

- [[../03-项目/OrbitOS/STATUS|OrbitOS]]：项目状态源已更新；当前处于单 agent / 多 agent 接入闭环、规则分层和 Dashboard 信息架构整理阶段。

### Agents

- 当前已注册：Codex、Nova、Hermes。
- Hermes 今日完成 Agent Onboarding，已接入 OrbitOS；runtime 为 `degraded`，不阻塞写入型 workflow。
- Nova 今日完成 Agent Onboarding，已接入 OrbitOS。
- 新 agent 接入流程已验证：Startup Sync 只读，Agent Onboarding 写 registry 和 Agent Profile，闭环完整。
- Nova 的 sandbox/pwsh 踩坑和 edit 参数错误已写入 `00-系统/agents/nova.md`。
- 后续 agent 启动时必须回读自己的 Agent Profile，让经验和踩坑进入行动约束。
- Agent Profile 已建立，后续需要把 agent 自己产生的待确认来源放回各自 profile。
- Rule 资产的详细积累优先保留在各 agent profile，learned index 只做系统级总表。
- Progress Sync 现在会先执行 Experience Capture 自检，再视情况触发 Rule Evolution 检查。
- checklist 正常项不展开到今日；只有 failed、blocked、需要用户知道的 skipped、待确认和关键状态会投影。

## 6. 来源

- 本页由 Progress Sync 根据当前对话、系统文件和 `.orbitos/logs/events/` 刷新。
- 机器事实记录保存在 `.orbitos/logs/events/`,不在 Dashboard 逐条展开。
- 用户可读的系统说明入口见 [[../00-系统/MAP|系统地图]]。
