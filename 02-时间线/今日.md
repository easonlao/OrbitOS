---
title: 今日
area: timeline
purpose: status
lifecycle: active
created: 2026-06-11
updated: 2026-06-12
tags:
  - orbitos
  - timeline
---

# 今日

## 当前状态

- OrbitOS 已完成首版骨架，正在补齐单 agent 接入前的系统约束。
- `今日.md` 作为当前总 Dashboard 使用；其他时间线文件是展开索引。
- Strict schema 最小 eval 已通过：8 个 case 全部符合预期。

## 正在进行

- 建立严格 schema、Validate Sync、Progress Sync、Inbox Triage 和最小 eval。

## 今天发生

- 确认 event 是事实日志，不等于生命周期。
- 确认收件箱第一轮只做方向性盘点，不移动、不删除、不沉淀。
- 确认 schema 走严格可校验格式，并配套验证脚本。
- 更新 GitHub-facing `README.md` 和 `README.zh-CN.md`，同步当前 schema/workflow/eval 状态。
- 重新对齐 README 定位：用户只需要知道如何开始使用、主要看哪里、系统说明书在哪里；内部实现放到二次开发部分。
- 新增 README 编写规范，并把它接入 `.orbitos/AGENTS.md` 的开发指引。
- 明确系统文档提升规则：讨论和设计先进入 `.orbitos/docs/`，确认后再以用户视角进入 `00-系统/`。
- 新增 core-change schema/workflow，用于约束 `.orbitos/` 内核编写和修改。
- validation eval 已扩展到 8 个 case，并通过 core-change 缺少 validation 必须失败的检查。
- 新增版本控制、commit、release 规范；完整 changelog 移到 `.orbitos/CHANGELOG.md`，`00-系统/CHANGELOG.md` 只展示当前 release。
- 收窄 `00-系统/CHANGELOG.md`：移除验证状态和下一步，只保留当前版本用户需要知道的变化。
- 重写 `00-系统/DATA-LIFECYCLE.md`：从用户在 Obsidian 里看到的目录流动解释数据生命周期，减少内部术语。
- 将 `OBSIDIAN-STANDARD.md` 从 `00-系统/` 迁移到 `.orbitos/docs/`，明确它是 agent 写作约束，不是用户说明书。
- 修正 `.gitignore`：个人输入、资源、输出、归档、OH-Works 和运行日志默认不再上传 Git。
- 新增 Git 管理规则，要求 agents 创建新内容前先判断公开系统材料、用户数据和运行时状态边界。
- README 增加下载/克隆仓库，并用 Obsidian 打开 vault 的上手说明。

## 待确认

- 暂无。当前 schema/workflow 首版已按对齐结论落地，后续通过 eval 和实际收件箱盘点验证。

## 可继续

- 用 `01-收件箱/` 的旧内容执行一次 inbox triage dry run。
- 根据 dry run 结果调整 triage 字段和 Dashboard 投影。

## 收件箱状态

- `01-收件箱/` 当前已有一批旧内容，包含交易方法、AI Agent/Obsidian、工作日志、系统运维、个人框架等主题。
- 首轮处理方式：生成 triage queue，投影摘要到本 Dashboard，不直接移动文件。

## 项目状态

- OrbitOS：系统骨架已建立，正在补齐 schema/workflow/eval。

## 系统变更

- 新增 event/lifecycle/inbox-triage/validation-report schema。
- 新增 validate-sync/progress-sync/inbox-triage workflow。
- 新增最小 eval cases 和 PowerShell 验证脚本。
- 已用 `pwsh -ExecutionPolicy Bypass -File .orbitos/scripts/run-validation.ps1` 验证通过。
- README 双语说明已从“骨架阶段”更新为“已有可运行系统基线”。
- README 双语说明已改为用户上手入口；中文 README 对已存在人读文件使用 Obsidian 双链，对 `.orbitos/` 使用普通路径。
- README 后续维护规则已沉淀到 `.orbitos/docs/README-WRITING.md`。
- `00-系统/` 与 `.orbitos/docs/` 的边界已写成内部规范和用户可见原则。
- OrbitOS 内核修改现在需要记录 reason、影响范围、验证方式和用户可见提升关系。
- `core-change.missing-validation.yaml` 已验证：缺少 validation 的内核变更会被拒绝。
- 当前版本定为 `v0.1.0`，表示首个可运行系统基线。
- 当前验证和下一步继续保留在 Dashboard，不放入用户可见 changelog。
- `DATA-LIFECYCLE.md` 已改为用户说明书视角：收件箱、时间线、确认、项目、知识、资源、输出、归档。
- `OBSIDIAN-STANDARD.md` 已从用户可见系统说明中移除，改为内部 agent Markdown 写作规范。
- Git 追踪边界已调整：公开仓库保留系统骨架和内核，个人内容目录只保留 `.gitkeep`。
- 根 `AGENTS.md` 已加入 Git Boundary，当前 88 行，仍低于 200 行。

## 阻塞

- 暂无。

## 来源

- 本页由 Startup Sync / Progress Sync 根据本地 event log 和已确认系统文档刷新。
- 机器事实记录保存在 `.orbitos/logs/events/`，不在 Dashboard 逐条展开。
- 用户可读的系统说明入口见 [[../00-系统/MAP|系统地图]]。
