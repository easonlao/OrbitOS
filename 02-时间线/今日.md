---
title: 今日
area: timeline
purpose: status
lifecycle: active
created: 2026-06-11
updated: 2026-06-13
tags:
  - orbitos
  - timeline
---

# 今日

## 1. 总览

### 当前判断

- OrbitOS 已有可运行系统基线:入口、时间线、Agent Profile、schema、workflow、eval 都已建立。
- 今天的主线是把 agent 入口、执行规则和人读 Dashboard 收紧,减少新 agent 误读和用户阅读负担。
- `今日.md` 是用户主入口;待确认事项应收拢在这里展示。
- 根 `AGENTS.md` 已重排为:启动顺序、工作流入口、规则入口、执行边界、同步与输出、开发边界。
- 新增 Agent Onboarding:真实 agent 第一次接入时,先只读 Startup Sync;未注册则停下收集字段,用户确认后再注册。
- Workflow checklist 已落地:workflow 定义核对清单,event 记录执行结果,今日只投影异常和关键摘要。
- 当前同步已按 checklist 真实执行,结果写入 event;今日只保留状态摘要。

### 当前阶段

- 单 agent 接入闭环继续打磨中。
- 新 agent 接入语义已收紧:不再叫"初始化数据库",改为"Agent Onboarding / 接入注册"。
- Startup Sync 已补充 Agent Profile 回读要求：已注册 agent 行动前必须读取自己的经验、踩坑、待确认来源和 Learned Rule 使用记录。
- Experience Capture 已作为 Rule Evolution 的前置输入层:agent 在出现错误、返工、用户纠正、validation 失败或明显有效做法时,必须先记录到自身 profile。
- Rule Evolution Workflow 已补齐闭环:agent profile 积累、learned index 总表、今日待确认投影、core 提升确认、失败回退。
- Workflow checklist 已形成最小闭环:规则、schema、eval、4 个核心 workflow、真实 event 全部落地。
- 今日收口判断:阶段 5 的 Role / Thinking Mode 尚未开始;阶段 5 前置的 Rule Evolution Workflow 已完成骨架,但还未完成 learned rule 输出链路。

### 系统健康

- `AGENTS.md` 当前 86 行，仍低于 200 行约束。
- validation eval 已通过：PowerShell 与 Node fallback 均为 `Validation eval passed: 16 case(s).`
- 可见层已扫描，未发现指向 `.orbitos/` 的 Obsidian 双链残留。
- Nova 暴露的 HanaAgent sandbox 限制已处理：`pwsh` 不可用时改用 `node .orbitos/scripts/run-validation.mjs`。

## 2. 待确认

### 当前需要你确认

- **systemd 服务管理方案与操作边界**:Hermes 管理 user systemd 服务的操作边界协定,建议后续归入 OrbitOS 运维子项目。
- **收件箱迁移方向**:Nova dry run 已完成 6 个主题簇、57 个文件盘点,需要你确认后才移动或沉淀内容。
- **明日审查方向**:当前 Startup Sync、Progress Sync、Experience Capture、Rule Evolution 的关联可能偏重,建议明天专门判断哪些行为需要解耦。

### 规则判断

- `今日.md` 应直接展示当前待确认事项。
- `02-时间线/待确认.md` 不再作为日常入口;后续可降级为归档/backlog,或迁移到各 agent profile。
- 每个 agent 的待确认来源应记录在自己的 `00-系统/agents/{agent_id}.md` 中,再由 Progress Sync 投影到今日。

## 3. 今日进展

### 收件箱盘点

- Nova 执行收件箱 dry run：scan → classify → triage queue → validation → Progress Sync，全链路跑通。
- 前次报告数量从 48 修正为 57，已同步 queue/event/Dashboard。
- 发现关键特征：大部分文件已有 frontmatter workspace 标注，预计可直接按标注迁移。
- 提出 5 个待确认事项（归属结构、目录关系、工作量较大的去向建议）。

### Agent 接入

- Nova 完成 Agent Onboarding：通过 HanaAgent sandbox 接入，IP 10.10.10.70。
- validation eval 已新增 Node fallback；两套脚本都覆盖 eval、可见 Markdown 双链边界和真实 registry。
- Agent Onboarding 闭环已验证：未注册→停止→确认字段→注册→Progress Sync，全链路跑通。

### Agent 入口

- 重排根 `AGENTS.md`：将工作流入口和规则入口分开，避免 rules/workflows 混插。
- 保留 Startup Sync、Experience Capture、Progress Sync 和开发边界要求。
- 新增 `.orbitos/workflows/agent-onboarding.md`：用于注册已确认的新 agent。
- 在根 `AGENTS.md` 补充新 agent 第一次接入提示词，要求未注册时停止写入并先询问 `agent_id`、部署位置、局域网 IP、接入方式和 OrbitOS 路径。
- 在 `README.zh-CN.md` 和 `README.md` 补充用户可复制的新 agent 首次接入提示词。
- 更新 `.orbitos/workflows/startup-sync.md`：补充"不清楚下一步时"的默认输出。

### 规则分层

- 将执行规则从 `.orbitos/docs/` 迁移到 `.orbitos/rules/core/`:
  - `markdown-writing.md`
  - `readme-writing.md`
  - `git-management.md`
  - `versioning.md`
  - `doc-promotion.md`
- 新增 `.orbitos/docs/README.md`,明确 docs 只放需求、架构、设计理由、未决权衡和实现解释。

### 可见文档

- 修正 `00-系统/MAP.md` 中指向 `.orbitos/docs/` 的 Obsidian 双链,改为普通路径。
- 重写 `今日.md` 为层级式 Dashboard,降低用户阅读混乱。
- 新增可见 Markdown 链接边界 eval:fixture 覆盖 invalid/valid,并扫描真实可见层,禁止指向 `.orbitos/` 的 Obsidian 双链。
- 新增 workflow checklist 标准和 event `checklist` 字段;本次 Progress Sync 已用 checklist event 真实跑通。

### Workflow checklist

- 新增 `.orbitos/rules/core/workflow-writing.md`,规定 workflow 必须有执行清单。
- `event.schema.yaml` 新增可选 `checklist` 字段,状态限定为 `done/skipped/failed/blocked`。
- 新增 `event.invalid-checklist-status.yaml`,非法 checklist 状态会被 eval 拒绝。
- `startup-sync`、`progress-sync`、`experience-capture`、`rule-evolution` 已加入执行清单。
- 本次同步生成了带 checklist 的真实 event:`.orbitos/logs/events/20260613_214025_workflow-checklist-baseline.yaml`。

### 路线图收口

- `03-项目/OrbitOS/main/ROADMAP.md` 已更新为当前真实进度。
- 阶段 1 基本完成,继续用真实 agent 验证。
- 阶段 2 dry run 已完成,等待用户确认后才迁移。
- 阶段 3 正在进行,今日已明确项目 STATUS 是状态源。
- 阶段 5 未开始;阶段 5 前置 Rule Evolution Workflow 骨架已完成,输出链路待验证。

## 4. 可继续

### 推荐下一步

- 确认收件箱 6 个主题簇的去向方向，批量迁移。
- 用一次真实踩坑触发 Experience Capture,验证 profile -> 今日 -> Rule Evolution 的输入链路。
- 明天审查 workflow 耦合度:哪些动作必须自动串联,哪些应改成按需触发。

### 可并行推进

- 复核 Nova 的 inbox triage queue,确认是否按 6 个主题簇迁移。
- 用 Codex/Hermes/HanaAgent 的真实经验试填 `.orbitos/rules/learned/INDEX.md`。
- 用下一个真实 workflow 继续验证 event checklist 到 `今日.md` 的投影规则。

### 暂不推进

- Role card / thinking mode / Hindsight Bridge 暂不推进,等核心 workflow 稳定后再做。

## 5. 各区状态

### 收件箱

- `01-收件箱/` 已完成首轮全量盘点（Nova, 2026-06-13）。
- 识别 6 个主题簇共 57 个文件：
  - 交易与做市商方法论（29 件）→ 知识候选
  - Obsidian/AI/Agent 集成（6 件）→ 知识候选
  - 工作日志（5 件）→ 时间线候选
  - 人选天选论（10 件，含 书/目录 8 文件）→ 资源候选
  - 系统与技术笔记（6 件）→ 分散归属
  - 民爆行业分析报告（1 件）→ 资源候选
- 完整文件级建议保存在 `.orbitos/queues/inbox-triage/20260613_215600_inbox-triage-dry-run.yaml`。
- 待用户确认去向方向后执行迁移。

### 项目

- [[../03-项目/OrbitOS/STATUS|OrbitOS]]：项目状态源已更新；当前处于单 agent / 多 agent 接入闭环、规则分层和 Dashboard 信息架构整理阶段。

### Agents

- 当前已注册：Codex、Nova。
- Nova 今日完成 Agent Onboarding，已接入 OrbitOS。
- 新 agent 接入流程已验证：Startup Sync 只读，Agent Onboarding 写 registry 和 Agent Profile，闭环完整。
- Nova 的 sandbox/pwsh 踩坑和 edit 参数错误已写入 `00-系统/agents/nova.md`。
- 后续 agent 启动时必须回读自己的 Agent Profile，让经验和踩坑进入行动约束。
- Agent Profile 已建立，后续需要把 agent 自己产生的待确认来源放回各自 profile。
- Rule 资产的详细积累优先保留在各 agent profile，learned index 只做系统级总表。
- Progress Sync 现在会先执行 Experience Capture 自检，再视情况触发 Rule Evolution 检查。
- checklist 正常项不展开到今日；只有 failed、blocked、需要用户知道的 skipped、待确认和关键状态会投影。

## 6. 来源

- 本页由 Progress Sync 根据当前对话、系统文件和 `.orbitos/logs/events/` 刷新。
- 机器事实记录保存在 `.orbitos/logs/events/`,不在 Dashboard 逐条展开。
- 用户可读的系统说明入口见 [[../00-系统/MAP|系统地图]]。
